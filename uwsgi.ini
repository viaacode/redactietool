[uwsgi]
module = wsgi:app

master = true 
processes = 20 

http = 0.0.0.0:8080 
vacuum = true
enable-threads = true
die-on-term = true

# Suppress access log noise from liveness probe and static file requests
log-drain = /health/
log-drain = /static/
